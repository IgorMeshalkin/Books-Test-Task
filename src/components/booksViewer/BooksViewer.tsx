import React from 'react';
import BooksList from "./booksList/BooksList";

const BooksViewer = () => {
    return (
        <>
            <BooksList/>
        </>
    );
};

export default BooksViewer;